<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all','data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application','data-turbolinks-track': 'reload' %>
    <!--<link rel="manifest" href="/manifest.json.erb" />-->
    <link rel="manifest" href="<%= asset_path('manifest.json.erb') %>" />
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#000000"/>
    <%= favicon_link_tag('favicon.ico') %>
  </head>
  <body>
    <div class="container-fluid">
      <%= render 'layouts/header' %>
    </div>  
    <div class="row search_block modal fade" id="sampleModal">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
    				<button type="button" class="close" data-dismiss="modal"><span>×</span></button>
    				<h4 class="modal-title">検索フォーム</h4>
    			</div>
    			<div class="modal-body">
            <%= search_form_for(@q, url: search_microposts_path, html: { method: :post })  do |f| %>
            
              <div class="form-group">
                <%= f.label :name_cont, "＜商品名検索＞" %>
                <%= f.search_field :name_cont, class: "form-control" %>
              </div>
              
              <div class="form-group">
                <%= f.label :genres_id_in, '＜カテゴリー検索＞' %>
                <br>
                <%= f.collection_check_boxes :genres_id_in, @genres, :id, :name %>
              </div>
              <div class="form-group">
                <%= f.label :college_id, '＜大学検索＞' %>
                <br>
                <%= f.search_field :user_college_name_cont, class: "form-control" %>
              </div>
              <div class="form-group">
                <%= f.label :price, '＜値段検索＞' %>
                <div class="form-inline">
                  <%= f.number_field :price_gteq, class: "form-control" %>円〜
                  <%= f.number_field :price_lteq, class: "form-control" %>円
                </div>  
              </div>
              <div class="action">
              <%= f.submit "この条件で検索する", class: 'btn btn-success btn-xs' %>
              </div>
            <% end %>
          </div>  
        </div>  
      </div>
    </div>      
      <div class="wrapper-container">
        <% flash.each do |message_type, message| %>
          <div class="alert alert-<%= message_type %>"><%= message %></div>
        <% end %>
        <div class="container">
          <%= yield %>
        </div>
      
        <%#= debug(params) if Rails.env.development? %>
        <div class="container">
          <%= render "layouts/footer"%>
        </div>
      </div>  
    </div>
  </body>
</html>