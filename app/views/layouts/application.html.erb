<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all','data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application','data-turbolinks-track': 'reload' %>
    <!--<link rel="manifest" href="/manifest.json.erb" />-->
    <link rel="manifest" href="<%= asset_path('manifest.json.erb') %>" />
    <link rel="apple-touch-icon" href="assets/serviceworker-rails/Icon-192.png" sizes="192x192"/>
    <script data-ad-client="ca-pub-3358183092034105" async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#000000"/>
    <%= favicon_link_tag('favicon.ico') %>
  </head>
  <body>
    <div class="container-fluid">
      <% if browser.device.ipad?||browser.device.iphone?||browser.device.ipod_touch?||browser.device.mobile?||browser.device.surface?||browser.device.tablet? %>
        <%= render 'layouts/mobile_header' %>
      <% else %>
        <%= render 'layouts/header' %>
      <% end %>
      </div>  
      <%= render 'layouts/serach'%>
      <div class="wrapper-container">
        <% flash.each do |message_type, message| %>
          <div class="alert alert-<%= message_type %>"><%= message %></div>
        <% end %>
        <div class="container">
          <%= yield %>
        </div>
      
        <%#= debug(params) if Rails.env.development? %>
        <% if false %>
          <div class="container">
            <% unless browser.device.ipad?||browser.device.iphone?||browser.device.ipod_touch?||browser.device.mobile?||browser.device.surface?||browser.device.tablet? %>
              <div class="mobile_footer">
                <%= render 'layouts/mobile_footer' %>
              </div>
            <% else %>
              <%= render 'layouts/footer' %>
            <% end %>
          </div>
        <% end %>
      </div>  
    </div>
  </body>
</html>