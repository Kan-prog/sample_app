<% if user.email.split("@").last.include?(user.college.last_email) %>
  <% permit_name = fa_icon("check-circle", text: user.name)%>
  <% user_name = permit_name %>
  <%= user_name %>
<% else %>
  <% user_name = user.name %>
  <%= user_name %>
<% end %>